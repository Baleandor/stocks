export type StockSliceType = {
  name: string;
  open: string;
  high: string;
  low: string;
  volume: string;
  latestTradingDay: string;
  previousClose: string;
  change: string;
  symbol: string;
  price: string;
  changePercent: string;
};
